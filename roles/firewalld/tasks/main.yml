---
- name: firewalld add IP white list
  firewalld:
    rich_rule: rule family=ipv4 source address={{ IP }} port port={{ PORT }} protocol="tcp"
    permanent: yes
    immediate: yes
    state: enabled
  become: yes